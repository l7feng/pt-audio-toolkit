# 更新日志

按**仓库级版本**记录整体发布；每个工具的细粒度版本史见其 `README.md` 或源码顶部注释。

> 仓库级版本（`VERSION` 文件）= 发布出的这一整套 exe 的版本；
> 工具级 `APP_VERSION` 各自独立演进。两者的对应关系见下表。

| 仓库版本 | 日期 | 工具版本（pt-tools / folder-builder / jianying / rename-unify） | 要点 |
|---|---|---|---|
| **2.9.0** | 2026-09-26 | **1.6.2** / 1.2.0 / **2.9.1** / 1.6.0 | 🔴 修 pt-tools 双击即崩（`_DND_BASE` 未定义，已发布三个版本的 exe 全中）· J11 人声分离页签落地（Demucs 两轨/四轨，模型外置）· 剪映启动崩溃回溯钩子 · 🔴 修 WAV 质检死循环（PT 真实 bounce 产物必挂）· verify_exe 增加崩溃框判据 |
| **2.8.0** | 2026-09-26 | 1.6.0 / 1.2.0 / **2.9.0** / 1.6.0 | J11 人声分离页签（分离逻辑部分）· 剪映 `sys.excepthook` 崩溃回溯钩子 · 遗留项脱敏（`pt_batch_export.py` fallback 项目名、`env/video_duration.py` 硬编码路径改参数） |
| **2.7.0** | 2026-09-25 | **1.6.0** / 1.2.0 / **2.8.0** / 1.6.0 | 方案 C 功能包：P1 交付包一步直出（扫描建档页）· J8/J9 剪映③交付包页退役（导入工具/说明退役） · J1 命名模板可命名（三栏管理弹窗） · J10a 分包夹名模板化 · J10b 字幕导出 srt |
| **2.6.6** | 2026-09-25 | **1.5.2** / 1.2.0 / **2.7.2** / **1.6.0** | 方案 B 体验包：G2 导出页滚动容器 · G1 拖拽推广 · J6 格式/码率/重名拆行 · J4 出厂默认 wav/320+一次性迁移+码率联动置灰 · P4 PT 版本适配常驻条 · R1 窗口可缩到 880×560 · F3 出厂提示去个人项目名 |
| **2.6.5** | 2026-09-25 | **1.5.1** / **1.2.0** / **2.7.1** / 1.5.0 | 方案 A 快修包：F2 按钮区永远可见 · F6 从根文件夹提取项目名 · F3 默认名「测试」 · J2 AAF 门控文案解释 · J7 零片段结构化诊断 · P2 格式文案重写 + 六弹窗居中 |
| **2.6.4** | 2026-09-24 | **1.5.0** / **1.1.0** / **2.7.0** / **1.5.0** | 07 清单全量执行：W1 pt-tools 拆三层 · W2 日志+诊断包 · W4 配置统一 exe 旁 · P1 离线黄条 · P2 导出自动质检 · P3 档案库页 · P4 导出历史 · W8 --batch CLI · J1 版本防线+一键备份草稿库 · J2 导入逐轨校验 · R1 审计快照+跨会话回滚 · R3 规则集导入导出 · F1 建立前预检 |
| **2.6.3** | 2026-09-24 | **1.4.0** / **1.0.1** / **2.6.3** / 1.3.0 | 四工具出厂默认路径收口（Jianying-Backup / PT-Tools-Backup）· pt-tools 档案平铺自动命名 · 指定工程文件预填父级 · 剪映导出页输入源合并 · folder-builder 界面分区重排 · 修 apply_config 崩溃 |
| **2.6.2** | 2026-09-24 | **1.3.0** / 1.0.0 / **2.6.2** / 1.3.0 | 13 项报障修复：批量导出可中止（修卡死）· 默认不再静默下混 · 逐轨声道 · 剔除空轨可撤回 · 路径锁定与预览 · 视频候选链 · 剪映下拉只读化与去英文 |
| **1.4.0** | 2026-09-23 | **1.1.0** / 1.0.0 / 2.5.1 / 1.3.1 | pt-tools 导出模式多选改造（MIX/BUS/STEM/按轨道名称）+ 按视频填入时间范围 |
| **1.3.1** | 2026-09-23 | 1.0.0 / 1.0.0 / 2.5.1 / **1.3.1** | Demo 无集数工程识别（用户报障修复） |
| **1.3.0** | 2026-09-23 | 1.0.0 / 1.0.0 / **2.5.1** / **1.3.0** | rename-unify 日期 mtime 兜底 + 集目录改名 + 出厂分派定案；剪映片段模式按视频窗归类 |
| **1.2.0** | 2026-09-23 | 1.0.0 / 1.0.0 / **2.5.0** / **1.2.0** | rename-unify 目标化重写；剪映视频名提取 + 按视频分包 |
| 1.1.0 | 2026-09-23 | 1.0.0 / 1.0.0 / 2.4.0 / 1.1.0 | 工程配套：测试入库、构建统一、版本口径统一 |
| 1.0.1 | 2026-09-23 | （当时工具级版本未定义） | 修 3 处缺陷 |
| 1.0.0 | 2026-09-21 | — | 首次聚合发布（四工具） |

> ⚠️ 登记断档说明：**1.5.0 → 2.6.1 期间仓库版本直接沿用 jianying 的工具版本号带飞**，
> 本表未逐条登记（可从 `git log` 回溯：`68b890c feat(v1.5.0)`、`3f40f29 v2.6.1`）。
> 自 **2.6.2** 起恢复逐版登记，历史不补写、不臆造。

---

## 3.0.0 — 2026-09-27（D 档架构包：工具合并 + 交付格式正式化）

> 33 项反馈的**最后一块**：A/B/C 三批与 J11 早已落地，漏掉的 D 档（合并 + 交付格式正式化）本轮补上。
> 依据：[[4-项目/12-pt-audio-toolkit仓库维护/16-2026-09-27-工具合并与遗漏项整改方案]]（实测审计以 git log + 源码为证）。

### 工具合并（F7+R2 · 甲案）

- **folder-builder 并入 rename-unify**，成为第 5 页签「5 · 工程文件夹」；`pt-project-folder-builder` exe 随本次发布**退役**（源码目录保留在 git 历史中）。
- 建树逻辑抽为纯逻辑模块 `src/rename-unify/code/foldertree.py`（**零 tkinter**，可独立测试），GUI 只收集参数后调用 —— 沿用 rename-unify「壳不动芯」。
- 字段链收敛（Q4 判定三套命名字段链同构）：页5 的项目名**默认取页1 的「片名」**，两处不再各填一遍。
- **F1**：「PT 工程文件夹生成器」这一标题随工具退役消失，页签名为「工程文件夹」。
- **F4**：设置并入 rename-unify 的 `config.json`（新增 `foldertree` 段，**逐键合并**落盘：缺键补默认、不覆盖用户值），不再各存一套。
- **F5**：删「项目根」选项 —— 集数文件夹**固定**建在 `Project/` 子目录下（`EP_PLACEMENT_DEFAULT`）。
- **G1**：页5 模板/输出路径框支持文件夹拖入（tkinterdnd2 缺席时安全回退）。
- **F6** 保留：「从上次项目提取」（`infer_fields_from_output`，与页1 字段链同源）。
- 铁律沿用：copy 步骤**先确认源可复制再动目标**（早期「先 remove 后判源」会不可逆丢失），已由测试守住。

rename-unify **1.6.0 → 1.7.0**；出口工具数 **5 → 4**。

### 交付格式正式化（决策 3 后半）

- 事实格式正式定名 **PT-Deliver**（json 清单 + `audio/` 目录，不做二进制）。
- 新增校验器 `src/pt-tools/ptools/core/deliver.py`：结构校验（纯 json）+ 包校验（素材存在性 / md5 / 时长对齐）+ `upgrade()` 兼容读取。
- 规范文档：`docs/pt-deliver-spec.md`（字段说明、`schema_version` 兼容规则、产出/消费方）。
- **既有产物无需迁移**：`schema_version: "1.1"` 的 `pt-clips.json` 本身就是合法 PT-Deliver 1.1 包（实测法老35 / 18 素材 / 31 轨 直接通过）。
- AAF 保留为「对外交换」可选项，与 PT-Deliver 不冲突。

### 测试

- 新增 `tests/test_foldertree_merge.py` **9/9**（命名拼装 / 集数解析 / F6 反解 / F5 Project 子目录 / 建树端到端 / copy 铁律 / 预检 / 配置逐键合并 / 分层与接线）。
- 新增 `tests/test_pt_deliver.py` **6/6**（结构校验 / 悬空引用拦截 / 1.0→1.1 补齐 / 包校验 md5 / 摘要 / 真实产物兼容）。
- 全量 **14 套 + GUI 冒烟 4/4** 全绿。

## 2.9.0 — 2026-09-26

**🔴 本版本含一个「已发布 exe 双击即崩」的修复，升级前请重装本版本。**

### pt-tools 1.6.0 → 1.6.1（P0 崩溃修复）

- **症状**：`pt-tools.exe` 双击无窗口，弹出 PyInstaller 错误框
  `Unhandled exception in script`（进程仍在）。
- **根因**：`ptools/gui/app.py` 的拖拽基类守卫写成了
  `class App(_DND_BASE if _TKDND_OK else tk.Tk)`，而 `_DND_BASE` **从未被赋值**
  ——try 分支只置了 `_TKDND_OK = True`。于是**只要机器上存在 tkinterdnd2**
  （含全部已发布 exe，PyInstaller 会把它打进 `_internal/`），模块 import 期
  就抛 `NameError`；tkinterdnd2 缺席时反而因三目短路不报错，属「装依赖才崩」的隐蔽型。
- **影响面**：`v2.6.6-20260925` / `v2.7.0-20260925` / `v2.8.0-20260926`
  三个版本的 pt-tools exe 全部不可用（B 批 G1 引入，回归测试当时被环境掩盖）。
- **修复**：try/except 之后统一 `_DND_BASE = TkinterDnD.Tk if _TKDND_OK else tk.Tk`，
  两条路径都有值；`class App(_DND_BASE)`。
- **为什么测试没抓到**：`tools/verify_exe.py` 的真启动判据是
  「进程存活 + 枚举顶层窗口 + 无 `tk` 残留空窗」——崩溃框**本身就是窗口**，
  进程也**确实存活**，于是判 OK。现补 `CRASH_TITLES` 判据
  （`Unhandled exception in script` / `Failed to execute script` / `Python error`）
  命中即 FAIL。

### jianying-draft-toolkit 2.9.0 → 2.9.1

- **J11 人声分离页签**：Demucs 命令行调模型，`vocals`（两轨，推荐）/ `all`（四轨）
  两种模式；文件或整目录输入，产物落输出目录。
  **torch 不打进 exe**（~2.5GB），运行时检测，不可用则按钮置灰并提示 `pip install demucs`。
- **启动阻塞修复**：`_check_demucs()` 改后台线程执行。
  原实现在 `__init__` 里同步探测，冻结 exe 环境下会拉起 GB 级 import，
  阻塞 GUI 主线程 → 窗口迟迟不弹。
- **冻结环境守卫**：`sys.frozen` 时**绝不**用 `sys.executable` 去 `import demucs`
  ——那等于把整个 GUI 程序当子进程重跑一遍。
- **崩溃回溯钩子**：`main()` 装 `sys.excepthook = _dump_crash`，
  未捕获异常写 `jianying_crash.log`（exe 旁 + `%TEMP%` 双写）。
  `--windowed` 下 stderr 被丢弃、PyInstaller 崩框不带 traceback，此前只能解包反汇编。
- 清理：移除排障期临时埋点（原会往 `D:\My-Temporary\jy_trace.txt` 追加）。

### pt-tools 1.6.1 → 1.6.2（P0 质检死循环）

- **症状**：导出后「自动质检」在**真实 PT bounce 产物**上永不返回——
  界面照常跑、进度不动、质检结果永远出不来，后台线程永久泄漏。
- **根因**：`core/qc.py::read_wav_header` 遍历 RIFF 块时用 `fh.seek(csz)` 跳下一块，
  **把「块长」当成了「绝对偏移」**，等于跳回文件中部。PT 产物在 `data` 之后还有
  `regn` / `umid` / `DGDA` 元数据块，末尾残留 8 个零字节被读成
  `cid=b'\0\0\0\0' csz=0` → `seek(0)` 回到 RIFF 头 → 无限循环。
- **发现方式**：2026-09-26 做 PTSL 端到端实测，「法老35」工程 10 秒真 bounce 的产物
  一进质检就挂死。**此前测试用的合成小 wav 没有尾随块，所以从未复现。**
- **修复**：改用绝对位置推进 `pos = pos + 8 + csz + (csz & 1)`（奇数块补 1 字节
  padding），加 `hops > 4096` 畸形文件防御，异常块大小按**剩余文件长度**裁剪。
- **回归**：新增 `tests/test_qc_p0_realwav.py`（**5/5**），复刻 PT 落盘布局
  （JUNK / bext / fmt / minf / elm1 / data / regn / umid / DGDA，含奇数长度块）
  并带**看门狗线程**——修复前是超时失败，不会把测试套件挂死。
  已反向验证：旧算法 6 秒不返回。

### 测试与工具链

- 新增 `tests/test_j11_separation.py`（**11/11**）：命令拼装、目录收集、空目录早退、
  非零返回码不中断、后台线程不变量、冻结守卫、崩溃钩子双写。
- `tests/_common.py`：`sys.stdout/stderr` 改 UTF-8 + `errors="replace"`。
  此前控制台 GBK 遇到 `✅` 会在**打印失败明细时**抛 `UnicodeEncodeError`，
  把真正的 FAIL 顶掉、报告中断（`test_core_logic.py` 一度直接崩在报告环节）。
- `tools/verify_exe.py`：崩溃框判据（见上）；`tools/build.py` 同样加编码护栏。

### 验证

- 全量 12 套：14 导入（13 PASS / 1 SKIP，无 ptsl）+ core 30 + wiring 5 + modes 25
  + videoname 12 + round06 9 + round_a 11 + round_b 10 + round_c 12 + **j11 11**
  + GUI 冒烟（见发布记录）。
- `verify_exe`：四 exe 真启动全过、零 tk 残留空窗。

---

## 2.8.0 — 2026-09-26

- **J11 人声分离页签**（分离逻辑部分，后续版本补非阻塞与守卫）。
- 剪映 `sys.excepthook` 崩溃回溯钩子。
- 遗留项脱敏：`pt_batch_export.py` 的 fallback 项目名 `誓言{id}` → `job-{id}`；
  `env/video_duration.py` 硬编码个人路径改命令行参数。
- ⚠️ 本版本**不可用**：pt-tools exe 因 `_DND_BASE` 未定义一启动就崩，
  请直接升到 2.9.0。

---

## 2.7.0 — 2026-09-25

**方案 C 功能包（33 项反馈 · 四阶段工作流第三批）**。方案见知识库
`4-项目/12-pt-audio-toolkit仓库维护/12` 号文档。

### pt-tools 1.5.2 → 1.6.0

- **P1 交付包一步直出**：扫描建档页新增「生成交付包」区块 —— PTSL 扫描
  当前工程 → pt-clips.json + audio/ 素材收集（L/R 预合成立体声）→ 产出
  「解压即用」交付包，一气呵成。实现走 pt-clips 技能（`skills/pt-clips/`，
  venv 子进程，与 pt_scan 同框架、可中止、日志实时）；仅数据包（导入工具
  exe 与使用说明不再随包）；素材缺失逐条列出、绝不静默出包。
  ⚠️ 同源约定：pt-clips 技能内三件套与剪映 `code/` 同名模块同源，
  改动需双向同步（真源归一登记在 D 批工具合并）。

### jianying-draft-toolkit 2.7.2 → 2.8.0

- **J8/J9 ③交付包页退役**：交付包由 pt-tools 直出后，③页移除；
  `make_delivery_package.py` 从本工具退役（核心入 pt-clips 技能）。
  **导入端能力全部保留**（交付包 json 完全离线导入 / `_delivery_package`
  识别 / 离线素材寻址），"导入工具.exe + 使用说明"叙事同步清除。
  关于页 / 「关于」帮助 / 导入页提示文案全部更新为新链路。
- **J1 命名模板可命名**：模板库条目升级 `{name, template}`（旧字符串条目
  自动迁移，名字=模板串前 12 字符）；新增「管理…」弹窗——
  **模板名 / 模板内容 / 样例** 三栏（样例实时把 {字段} 展开为示例值）；
  新建/删除/保存齐全；勾选与渲染链路零改动（`name_templates_active`
  与 `name_template` 兼容字段仍存模板串，CLI 无感）。
- **J10a 分包夹名模板化**：「按视频分包」的文件夹名不再固定视频名，
  支持模板（默认 `{视频名}` 保持现状），可用 `{视频项目}{集数}{编号}{AiFX}{视频名}`
  ——例如 `{视频项目}{集数}` 即「根据视频信息/集数打包文件夹」。
- **J10b 字幕导出**：新增「导出字幕（.srt，文本轨）」内容勾选（CLI
  `--subtitles`）——从草稿文本轨提取，整轨模式全时间线 / 分包模式按集
  窗口裁剪并平移时间码，落 `<集名>/字幕/<集名>.srt`，不依赖 ffmpeg。

### 打包

- pt-tools 内置技能从 7 → 11 个脚本（新增 pt-clips 技能四件）。
- 回归：新增 `tests/test_round_c_features.py` 12 项（模板迁移/字幕窗口
  裁剪/退役检查/技能就位），全量 11 套全绿。

---

## 2.6.6 — 2026-09-25

**方案 B 体验包（33 项反馈 · 四阶段工作流第二批）**。方案见知识库
`4-项目/12-pt-audio-toolkit仓库维护/11` 号文档。

### jianying-draft-toolkit 2.7.1 → 2.7.2

- **G2/J12 导出页滚动容器**：`tabs/base.py` 新增 `ScrollableFrame`（Canvas +
  滚动条 + 宽度联动 + **滚轮智能让位**——鼠标在日志框/列表框/树上时让它们自己
  滚），导出页（信息最密）先行套用；窗口不够高时右侧出滚动条，信息不再显示不全。
- **J6 布局拆行**：旧版「音频格式 / 码率 / 重名策略」三组控件挤在同一行
  （码率标签与格式下拉同列相贴，视觉上"码率框里出现了重名策略"）。拆为
  「格式+码率」一行（各占独立列）+「重名策略」独占一行；格式标签注明
  「（片段模式）」、码率标签注明「（仅 mp3）」。
- **J4 默认值与联动**：出厂默认 `mp3 / 192` → **`wav / 320`**（用户工作流
  的期望默认）；旧配置**一次性迁移**（`_migrated_266` 标记，只改仍是旧默认
  的值，迁移后用户特意改回不会被反复覆盖）；**WAV 下码率框自动置灰**
  （WAV 是无损 PCM，码率不适用）。

### pt-tools 1.5.1 → 1.5.2

- **P4 PT 版本适配常驻条**：窗口顶部新增灰色说明条（黄条之上，常驻）——
  「已验证：PT 25.6.1 · PTSL v6 · py-ptsl 602.0.0 · 端口 31416；其他 PT
  版本未经完整验证；换设备/账号不受影响」。面向开源用户的版本边界声明，
  README 同步版本适配表。
- **G1 拖拽**：导出页「档案（pt-profile.json）」与「输出目录」两个输入框
  支持拖入（档案拖入直接走既有加载链）。tkinterdnd2 缺席时安全回退。

### rename-unify 1.5.0 → 1.6.0

- **G1 拖拽**：页 3「目标目录」输入框支持拖入，拖入即刷新预览。
- **R1 窗口可缩放**：minsize 从 1080×700 下调到 **880×560**——页内树/列表
  各自带滚动，小窗口依然可用，窗口可以随意收小放到屏幕边角。

### 通用

- **F3 尾巴**：folder-builder / pt-tools 出厂提示与示例文案去除个人项目名
  （默认项目名已在 2.6.5 改「测试」）；README 与注释中的历史实测示例保留，
  开源前的整体文档脱敏作为独立遗留任务登记。
- **打包**：pt-tools / rename-unify 的 PyInstaller 参数补
  `tkinterdnd2`（hidden-import + collect-data），拖拽能力随 exe 分发。

---

## 2.6.5 — 2026-09-25

**方案 A 快修包（33 项反馈 · 四阶段工作流第一批）**。问题清单、拍板与方案见
知识库 `4-项目/12-pt-audio-toolkit仓库维护/` 的 08~10 号文档。

### pt-project-folder-builder 1.1.0 → 1.2.0

- **F2 按钮区永远可见**：日志/按钮改为从底部预留（`side=bottom` 先占位），
  预览区最后 pack 收缩吃剩余——窗口再小「刷新预览 / 建立文件夹」不再被挤出
  可视区（旧版 800×780 默认窗口下必须放大全屏才能点）。预览 11→7 行、
  日志 6→4 行减压，默认窗口 860×860，minsize 800×620。
- **F6 根名提取**：项目名称为空时集名/ptx 只剩集数（`build_episode_name`
  的 name 段为空串）。新增「**从上次项目提取**」按钮——从输出根序号最大的
  项目根（如 `15-测试D_20260925_7F`）解析项目名/等级一键回填；不静默代填，
  日期/用户名保持当前值（新项目日期应随当天）；预览区在项目名为空时给出
  醒目警告。解析逻辑抽为纯函数 `parse_project_root_name` 并入回归测试。
- **F3 默认项目名**：出厂默认「誓言」→「测试」，不携带个人项目信息。

### jianying-draft-toolkit 2.7.0 → 2.7.1

- **J2 AAF 门控文案**：「同时导出 AAF」只在勾选**整轨**模式时可用——AAF 描述
  完整时间线，纯片段模式只有素材块、语义上无法生成（设计约束而非缺陷）。
  置灰时文案直接写明原因，不再让用户对着灰色复选框猜「为什么不能用了」。
- **J7 零片段诊断**：导入预演「0 可用片段、涉及素材 0 个」时输出结构化
  `[diag]` 诊断，按概率排序：① json 无 tracks 字段 → 疑似把工程档案
  （pt-profile.json）当片段清单导入（本批实测 `PT-Tools-Backup` 下只有
  档案没有片段清单，为最可能根因）② 有片段的轨道全部命中排除名单
  ③ 全部片段找不到素材文件。

### pt-tools 1.5.0 → 1.5.1

- **P2 格式文案**：「单声道（会下混）」→「单声道文件 · 立体声轨会被合并
  降混」等三条格式显示名重写（中英同步），消除理解障碍；语义不变（文件
  组织方式，非声道数）。
- **P2 弹窗居中**：全部 6 个 Toplevel（帮助 / 关于 / 剔除轨道 / 视频选择 /
  输出路径预览 / 批量导出）统一相对父窗居中，不再默认压在左上角盖住
  「导出模式」区块。

---

## 2.6.4 — 2026-09-24

**07 清单全量执行轮（D1 快赢 + D2 结构 + D3 功能 + D4 落地 + 附加）**。
人类口令「全部执行」＋四项拍板（J3 不迁目录、W4 配置统一 exe 旁、W8 本轮做 CLI、
W1 本轮拆三层）；范围与排除详见 `docs/` 与知识库 07 清单。

### 通用：四工具滚动日志（W2 骨架）

- 四工具统一接入 **RotatingFileHandler**：日志超过 1 MB 自动轮转，
  旧日志改名 `<名>.log.<序号>` 保留，不再无限膨胀（此前部分工具靠手动清空）。
- pt-tools「工具 → 导出诊断包」＝ 日志+配置+版本号 zip 一键打包，远程报障不再靠截图。

### pt-tools 1.4.1 → 1.5.0

- **W1 三层拆分**：单体 `pt_tools_gui.py`（3330 行）拆为 `ptools` 包——
  `core/`（settings / i18n / config / paths / naming / logs / notify / qc）、
  `gui/`（App + 页签 + main）、`worker/`（CmdWorker / PTSL 探测 / 诊断包 / 批量 CLI）；
  `pt_tools_gui.py` 收敛为薄壳 shim，对外 API 原样再导出，测试与打包无感。
  拆分说明见 `docs/pt-tools-3layer-split.md`。顺手修 2 处真问题：
  诊断包菜单回调误用 `App.win`（应传 `self`）、拆分期植入的 6 处 `???` 乱码复原。
- **P1 PT 离线黄条**：PTSL 不通时窗口顶部常驻黄条（在线自动收起），
  离线原因一眼可见，不再"点按钮才深处报错"。
- **P2 导出后自动质检**：真导出（非预演）完成后，后台核对本次落盘 wav 的
  声道/采样率/位深/时长（对比档案与导出参数），异常红字清单+托盘汇总；
  时长容差 1s 下限 + 2% 相对，抓"导错了还以为导对了"。
- **P3 档案库页**（新页签 3）：档案目录列表 / 搜索 / 双击加载到导出页 /
  打开所在目录 / 删除（走回收站，PowerShell VisualBasic，失败显式报错）。
- **P4 导出历史**：档案库页下方按修改时间列出输出根的工程文件夹，双击打开。
- **W4 配置迁移 exe 旁**：配置/日志/诊断包从 `%APPDATA%\pt-tools` 迁到 exe 旁
  （便携、随 exe 走，与剪映/rename-unify 一致）；首次运行自动复制旧配置，
  **只复制不删除**。
- **W8 `--batch` CLI**：`pt-tools.exe --batch <jobs.json>` 夜间批量导出
  （复用 pt_batch_export.py 编排链，壳不动芯）；输出落 exe 旁
  `pt-batch-<时间戳>.log`（--windowed exe 无控制台）。批量对话框新增
  「保存任务清单（jobs.json）…」按钮，白天配好夜里跑。

### jianying-draft-toolkit 2.6.4 → 2.7.0

- **W3 关闭确认**：窗口关闭时若导出/导入/交付任务进行中，弹确认提醒；空闲不打扰。
- **W7 托盘通知**：任务完成弹托盘气泡。
- **J1 版本防线三件套**：① README 标注已验证剪映版本（11.5.0，6.x 多时间线草稿结构）
  ② 启动时后台扫描草稿库各草稿 schema 版本，出现「没见过的版本」顶部挂黄条
  （点开可看分布、可「记为已知」；不硬崩）③「工具 → 一键备份草稿库…」：
  robocopy 整库复制到草稿库旁带时间戳的备份夹，升级剪映前先备份。
- **J2 导入写回后逐轨校验**：写回草稿并往返解密核验后，把「计划 vs 实际」
  逐轨核对（轨道在不在 / 片段数 / 总时长 ±2% / 素材引用悬空检测），
  CLI 与 GUI 日志都出报告；不一致显式告警并提示 .jybak 回滚路径。

### folder-builder 1.0.2 → 1.1.0

- 接入 `setup_logging` 滚动日志（1.0.2）。
- **F1 建立前预检**：确认框前一次性检查模板存在性 / .ptx 可读性与占用 /
  输出盘可写（探测文件即建即删），问题列全再让用户决定，不再"边建边炸"。

### rename-unify 1.4.0 → 1.5.0

- **R2 统计行**：批量改名完成后输出「共 X 文件·待改名·已合规·失败·冲突」
  统计行 + 失败清单，失败 ≥10% 提示补规则。
- **R1 执行前审计快照 + 跨会话回滚**：每次执行前把「原→新」计划落成
  `rename_audit_*.json`（覆盖"执行到一半崩了"的场景——回溯日志只在成功后写）；
  页4 新增「按审计回滚…」：选快照 → 确认 → 整批改回，与 CSV 撤销共用同一回滚核心。
- **R3 规则集导入导出**：页2「导出规则集/导入规则集…」一键打包
  字段/模板库/目标表/识别规则/两个开关为 json，两机同步不再依赖整仓 git。

### J3 工作目录声明（文档，不迁移）

- 用户拍板：**不迁移** `D:\My-Temporary\Jianying-Backup`。
- README 新增「工作目录说明」节 + 「关于」对话框声明：该目录是**工作目录**
  （草稿库 + 历史产物），虽带 "My-Temporary" 字样，但**不是临时目录，请勿删除**。

### 文档与测试

- 新增 `docs/versioning.md`（W5 版本双轨规则）、`docs/packaging-onedir.md`
  （W6 onefile/onedir 取舍与切换清单）、`docs/pt-gui-3layer-split.md` /
  `docs/pt-tools-3layer-split.md`（W1 拆分坐标与陷阱）。
- 新增 `tests/test_round06_features.py` 9 项：QC 头解析/质检清单/容差、
  W4 迁移幂等、W8 校验、J1 版本扫描、J2 逐轨校验、R1 审计往返/真回滚、F1 预检。
- 执行档位对应：D1（W3+W7+W2骨架+R2）→ 9c06c90；D2（W1+W2诊断包+J3 声明）→
  02aba36；D3/D4/附加 → 本版。

---

## 2.6.3 — 2026-09-24

**出厂默认路径收口 + 输入源合并 + GUI 整理**。需求来自用户当日清单（四工具默认路径 /
档案存放方式 / 两个输入源合并 / GUI 美化）；路径权威清单入库知识库
`06-2026-09-24-四工具默认路径总表.md`。

### 默认路径（出厂值，四工具）

- **jianying-draft-toolkit 2.6.3**：`input_dir` → `D:\My-Temporary\Jianying-Backup\Project\JianyingPro Drafts`、
  `output_dir` → `...\Tools\out`、`temp_dir` → `...\Tools\tmp`、
  `import_pkg_out` → `...\Tools\deliver`（log_dir / data_dir v2.6.0 起已在新位置，不变；
  `import_draft_dir` 语义是"钉死单个目标草稿"，保持留空，草稿下拉列表跟随 input_dir 草稿库）。
  仓库内源码模式 config.json 两份同步更新。exe 每次发新版都是全新目录，无需迁移代码。
- **pt-tools 1.4.0**：新增 `profile_dir`（`D:\My-Temporary\PT-Tools-Backup\json`，建档输出 +
  导出页浏览档案默认目录）；`last_out_dir` 缺省 → `D:\My-Temporary\PT-Tools-Backup\out`。
  ⚠️ pt-tools 配置在 `%APPDATA%\pt-tools\` **跨 exe 版本共享**，故走既有 `_migrate_cfg`
  （cfg_version 2→3），只补缺省、不覆盖用户已设值。输出/建档目录不存在时现建（makedirs）。
- **pt-project-folder-builder 1.0.1 / rename-unify 1.3.0**：默认路径本已合规
  （`D:\DAW-Project\00文件夹模板` / `D:\DAW-Project`），核对后未改动。

### pt-tools 1.4.0 功能

- **档案平铺自动命名（拍板方案 B）**：不建 per-project 文件夹；扫描完成后从档案取
  `session.name` 自动改名为 `<工程名>-pt-profile.json`（非法字符清洗，同名重扫覆盖，
  改名失败保留原名不阻断）。建档目录记忆键从 `last_out_dir` 拆出为 `profile_dir`，
  两个落点语义分离、互不带偏。
- **「指定文件」模式预填**：切到该模式或载入档案时，自动预填档案里记录的 `.ptx` 路径
  （= 扫描时 PT 打开的工程）；「浏览」对话框默认开在**其父级目录**。

### jianying-draft-toolkit 2.6.3

- **导出页两个输入源合并（五.2）**：旧版「剪映草稿目录」行 + v2.6.2 的「输入源」按钮框
  两处入口功能重复；合并为一个「输入源」块（手填 / 浏览目录 / 选择文件 / 清空 / 重新识别 /
  即时识别状态），区块顺序统一为 输入源 → 导出模式 → 导出配置 → 操作按钮 → 运行日志。
- **导入页草稿下拉跟随配置**：`default_draft_root()` 改为优先读配置的 `input_dir`
  （存在即用），否则回落 AppData 自动定位 —— 旧版写死 AppData，改了草稿目录对导入页不生效。
- **真 bug 修复**：`export_tab.apply_config()` 引用已被删除的 `self.var_template`
  （v1.2.0 改多选 Listbox 时遗留）→ 菜单「重新载入配置 / 默认路径设置」必崩
  AttributeError。已移除该行（name_template 由 collect() 从勾选模板派生）。
- 清理死代码 `_pick_input_dir`。

### pt-project-folder-builder 1.0.1

- **界面分区重排（GUI 美化）**：单页平铺 grid（row 0-14 混排）重排为 6 个 LabelFrame
  区块（路径 / 本批内容 / 命名规则 / 命名信息 / 选项 / 预览 / 日志），主按钮
  「建立文件夹」固定最右；控件、变量、逻辑不变，纯布局调整。
- pt-tools 与 rename-unify 布局本已结构化（Notebook 分页），本轮核对后**不做无效重排**。

---

## 2.6.2 — 2026-09-24

**用户一次性报障 13 条**（pt-tools 八条 + 剪映工具包五条）后的集中修复轮次。
对应诊断文档：`4-项目/12-pt-audio-toolkit仓库维护/04-2026-09-24-PT工具13项问题诊断与改造方案.md`。

### pt-tools 1.3.0

- **Q12 卡死（最高优先级，双根因已修）**
  - `CmdWorker` 重写：reader 线程喂队列 + 主线程轮询 `proc.poll()`，
    不再是「`for line in proc.stdout` 不可中断阻塞」；新增 **无输出 300s 判定卡死**
    并主动终止；新增 `cancel()`（置标志 → `terminate` → 等 1.5s → `kill_process_tree`）。
  - `kill_process_tree()`：Windows 走 `taskkill /T /F /PID`。
    ⚠️ 红线写进注释：**只杀本工具自己 spawn 的 venv python 子树，绝不碰 ProTools.exe**。
  - `App` 注册 `WM_DELETE_WINDOW` → `_on_close()`：有活任务先确认 → cancel → join →
    destroy → `os._exit(0)`（绕过 `subprocess` 的 atexit 等子进程）。
  - UI：日志区加**「中止」按钮** + 运行计时 Label；日志上限 5000 行自动裁剪。
- **Q5 默认不再静默下混（真 bug）**：默认导出格式 `mono` → **`interleaved`**。
  旧默认会把立体声静默混成单声道，用户完全无感。新增 `_migrate_cfg()`（cfg_version 1→2），
  **只修正仍是旧默认的值，用户手工改过的值不覆盖**。
- **Q5 非法值不再静默回落**：新增 `fmt_key_of()`，识别不了返回 **None**（旧行为是
  取首 token 查不到就回落默认），`build_export_cmds()` 抛 `ValueError` 显式报错。
- **Q5 逐轨声道（按用户追加要求）**：轨道列表新增第 5 列**「声道」**，
  点该列循环切换 `跟随默认 / interleaved / mono / multiple-mono`，
  `_group_by_format()` 按格式分组 → 一次勾选可产出多条命令。
  ⚠️ 说明：`interleaved/mono/multiple-mono` 是**文件组织方式而非声道数**，
  实际宽度由 PT 轨道/输出决定；**5.1 / 7.1 需 PT 侧输出为对应宽度的 bus**，
  工具侧按 `interleaved` 导出即可，不做虚假的"5.1 按钮"。
- **Q5 时长默认值**：视频余量 240 → **0**，兜底时长 240 → **60**；
  命中兜底时日志打**红字告警**（用户必须知道这是猜的值）。
- **Q7 剔除空轨道（可撤回）**：新增复选框 + 「查看被剔除的 N 条」对话框，
  可逐条**加回**；`_selected_sources()` 在勾选时过滤空轨。
- **Q3/Q6 路径锁定与预览**：输出目录加**锁定按钮**；新增「查看输出路径」
  只读预览（总数 / 被剔除数）。
- **Q1 视频检测候选链**：`.ptx` 同级 → 父级 → 祖父级（去重、只留真实存在），
  日志打印命中的根。修 `find_session_videos.py` 参数优先级
  （`--dir` 必须优先于 `--session`，否则父级候选永远走不到）。

### jianying-draft-toolkit 2.6.2

- **Q9/Q11 下拉框内容可删（真 bug）**：`_sync_mode()` 把建控件时的
  `state="readonly"` 改成了 `"normal"` → 一律改回 **`readonly`**
  （含 `cb_format` / `cb_bitrate` / 整轨规格 / AAF / 重名策略）。
- **Q9 静默回落（反模式）**：`_read_spec()` 取首 token 当 key、查不到回落默认且不报错
  → 改为识别不了就 **抛 `ValueError`** 弹框（用户以为选了 A 实际导出 B 是事故级）。
- **Q9 UI 重叠**：整轨规格 Combobox 与「整轨命名」Label 都占 row2/column1 →
  命名改 row3，边框显示完整。
- **Q9/Q11 去英文**：整轨规格（`48k24b_st` 等）、AAF 模式、重名策略
  （`rename/cover/skip`）**显示名全部中文化**，内部 key 与显示名用映射表分离
  （`SPEC_LABEL_TO_KEY` / `AAF_LABEL_TO_KEY` / `CONFLICT_LABEL_TO_KEY`），兼容旧裸 key。
- **Q10 取消拖拽区**：改为「输入源」框（选择草稿根目录/文件夹、选择文件、清空、
  重新识别）+ 即时摘要反馈；拖拽目标改为输入/输出目录输入框本身。
  修 `dropped_paths` 恒优先于 `input_dir` 导致「填目录不生效」——改**最后操作者优先**。
- **Q10 选目录识别不出**：`is_draft_dir()` 与自身文档矛盾（只认根目录三个 marker，
  但权威路径是 `<草稿>/Timelines/<uuid>/draft_content.json`）→
  补 `_has_timeline_content()` 判定，保留 `path.parent.name == "Timelines"` 排除。

### 文档

- 新增 `docs/ptsl-boot-plan.md`：PTSL 服务启动 / 自愈方案（Q2，脚本与 exe 的实现蓝图）。

### 质量

- 本机托管 Python 无 tkinter，仓库 `tests/` 无法在进程内启动 → 本轮以
  **临时探针 + 桩模块**验证纯逻辑（pt-tools 8 组 / 剪映 4 组，全绿），
  两侧 `py_compile` 通过。**GUI 冒烟与 `tests/` 全量回归仍待有 tkinter 的环境补跑。**

## 1.4.0 — 2026-09-23

**pt-tools 1.1.0 —— 导出模式多选改造**（用户四条需求：源类型看不懂 / 想一起选 /
时间默认视频长度 / 想按轨道名称导）。

### 导出页重构

- **「导出模式」四复选框多选**（旧「mix/stems 单选 + 源类型下拉」废弃）：
  **MIX 整段并轨**（ExportMix 主输出，1 个文件）｜**BUS 总线分轨**（ExportMix 全部
  bus，每条 1 个文件）｜**STEM 全部分轨**（BounceTrack 全轨，默认排除总线类轨 +
  空轨）｜**按轨道名称**（BounceTrack 指定轨，勾选后从档案轨道列表挑，新增类型列）。
  勾哪几路就按 MIX→BUS→STEM→TRACK 顺序各跑一条 CLI 命令，**失败即停**。
- 旧「源类型」三个值（bus/output/physicalout）不再暴露——physicalout（声卡硬件
  端口，内录语义）实际工作用不上；CLI 能力保留。
- **STEM 子选项「含效果辅助轨 (aux)」**：默认排除 aux；勾上改走
  `--track-type audio/aux/instrument/midi` 白名单（master/vca/folder 仍排除）。
  配套改 `pt_export.py::_filter_by_profile`：空轨过滤从仅 audio 扩到 audio+aux，
  避免含 aux 模式导出一批静音文件。
- **「按视频填入…」按钮**：选视频 → `video_duration.py`（纯标准库读 MP4/MOV mvhd）
  → 按工程帧率换算 → 一键填「开始 00:00:00:00 / 结束=视频末帧」。加载档案后结束
  时间默认仍是工程会话长度，想要精确对齐视频就点此按钮。
- **预览闸门补实装**：旧 UI 文案承诺「参数一变执行按钮熄灭」但代码从未比对签名——
  现给全部 14 个参数变量挂 trace，任何变化（含模式勾选/选轨）即熄灭执行按钮。
- **顺手修一个旧布局 bug**：格式（mono/interleaved）下拉与输出目录输入框 grid 同格
  重叠（实际被盖住看不见），重排参数区后正常显示。

### 技术与质量

- 命令构造抽成模块级纯函数 `build_export_cmds()`（GUI 与测试共用）；
  `CmdWorker` 升级为命令队列（分节标记 `[i/n]`、失败即停、单命令向后兼容）。
- 新增 `tests/test_pt_modes.py` **25 项**：四模式命令构造 / 组合顺序 / 校验链
  （空模式·未选轨·缺 output·缺 bus·时间·采样率·档案）/ 视频解析 / 队列失败即停 /
  模式联动冒烟。全量回归 **87 项全绿**（15 导入 + 30 核心 + 5 接线 + 12 视频名 + 25 专项），
  四工具 GUI 冒烟 4/4。
- ⚠️ 本轮发布**只重建 pt-tools**；其余三工具从 v1.3.1 出口原样复制到 v1.4.0 出口
  （代码零改动）。工作区遗留的 jianying v2.6.0 未提交改造**未包含**在本发布中。

## 1.3.1 — 2026-09-23

**Demo 无集数工程识别**（用户报障：`D:\DAW-Project\0-Demo\FinalMix\出轨Demo 0923 V02 7F`
的 12 个文件全部识别失败）。根因：「Demo」占着集数位但不是数字，旧规则
`{集数}`/`{裸集数}` 只认纯数字。

- **识别**：新增 Demo 规则族（全格式 + 短格式两组）——Demo 并入片名捕获
  （`出轨Demo`），ep 置空；短格式（已归位态 `出轨Demo DX BUS`，无日期可锚）
  靠「片名含 Demo」识别，否则二次扫描全 error。
- **日期归一化**：`normalize_date()` 8 位 `20260923` → 4 位 `0923`（用户手工
  标准答案的日期段是 4 位，与法老D 一致）；其余长度原样保留。
- **渲染折叠**：ep 为空时模板集数段整段折叠——全格式 `{片名} {集数}集 {日期}…`
  → `{片名} {日期}…`；短格式 `{片名} {集数}_{轨道信息}` → `{片名} {轨道信息}`
  （**连下划线一起折叠**，用户标准答案是空格分隔、无下划线）。
- **集目录改名放宽**：不再要求现名「像集目录」——`出轨/` 这类无集数工程目录
  同样归位到 `出轨Demo 0923 V02 7F/`（仍限 root 直接子目录）。
- **回归**：仓库测试 **30/30**；出轨 Demo 12 文件全部识别且判「已合规」不动；
  FinalMix 165 文件零干扰。
- **顺带发现**：`兔子新娘 1集 …`（未补零）目录会被统一为 `兔子新娘 01集 …`
  （normalize_ep 补零语义，v1.3.0 起即如此，预览可见）。

## 1.3.0 — 2026-09-23

同日第二轮改造（用户 2026-09-23 下午四条新需求，FinalMix 全量 108 文件回归验证）。

### rename-unify 1.3.0 —— 日期自动兜底 + 集目录改名

- **出厂模板分派定案**：MIX = 全格式，BUS/STEM/AIFX = 短格式
  （与用户手工成品 `法老 02集 0923 V01 7F\{,BUS,STEM}` 形态同构；
  17 集旧全格式 BUS/STEM 在此默认下会被改名，用户已拍板「统一成新风格」）。
- **日期 mtime 兜底**：源文件名与集目录都没有日期时，用**文件修改时间(MMDD)**
  自动补日期。根因是旧逻辑只能落到全局字段那一个写死的值，多日期批次全错；
  实测 FinalMix 全量 mtime 与用户手工标注 **100% 吻合**（12/13/17 集 → 0922，
  其余 → 0923）。优先级：源名捕获 > 集目录多数票 > 文件 mtime > 全局字段。
  GUI 页 3 可关（`date_from_mtime`）。
- **集目录改名**：`FinalMix\13\` 这类旧目录统一改名为
  `法老 13集 0922 V01 7F`（与 Master 文件名同构）；限 root 直接子目录，
  更深层结构不动。改名后旧目录空壳**只提醒、不代删**（`list_emptied_dirs`）。
  GUI 页 3 可关（`rename_ep_dirs`）。
- **真实数据回归**（只读预览）：108 文件 → 40 处变更（12 集 12 + 13 集 15 +
  17 集 13）、68 处已合规不动、0 错误 0 冲突，13 集 MON/dup1 疑似提醒生效。

### jianying-draft-toolkit 2.5.1 —— 片段模式按视频窗归类

- **片段模式**（音乐/音效等分类导出）现在也会解析视频轨：音频片段的时间线落点
  落在哪个视频片段区间，就归进哪个「集」的子文件夹（`music/法老6/…`），
  并把视频字段（{视频项目}/{集数}/{编号}/{AiFX}/{视频名}）注入片段命名模板。
- `AudioSegment` 新增 `tl_start_us`（时间线落点，取自 `target_timerange`）；
  新增 `chunk_index_for_tl()` 窗归属判定（返回列表下标）。
- 没有视频轨、或片段落在所有视频窗之外 → 维持旧的平铺行为。

### 清理

- 沙箱旧散装测试副本（与仓库版不同源的 `test_core_logic.py`）及 v1.2.0 时代
  预览脚本/报告已回收；exe 目录的 `rename-unify.prev-20260923-120419` 旧构建
  备份已回收（正式目录经 SHA256 对比确认是新版）。

## 1.2.0 — 2026-09-23

两个工具的功能改造（依据 `D:\DAW-Project\13-法老D_20260922_7F` 的真实文件回归）。

### rename-unify 1.2.0 —— 识别模型改为「目标 + 轨道信息」

- **识别引擎重写**：旧模型「一条规则 → 一个类型串（`BUS-DX` / `STEM-MX-02`）」在
  真实交付目录上 **20/28 样例识别失败**，其中包括**用户自己已交付的 17 集成品**
  （`法老 17集 0922 V01 7F_Ai FX 1.wav`、`…_MX Verb ST.wav` 等）。
  新模型产出**目标（MIX / BUS / STEM / AIFX）+ 轨道信息（原样保留的自由文本）**，
  同一批 85 个文件 **0 失败**。
- **`{档位}` → `{用户}`**：`7F` 是个人代号而非交付档位。旧 `config.json`
  的 `fields.档位` 与旧规则里的 `{档位}` **自动迁移**，不静默丢配置。
- **模板库 + 每目标各绑一套**（分派语义，不是复制）：Master 走全格式、
  BUS/STEM 走短格式或「保持原名」，互不干扰。
- **「保持原名」**：只归类不改名（对应 `FinalMix\12\` 那种「只改 Master」的状态）。
- **手动分类**：识别失败的项可在计划表里当场指定 集数 / 目标 / 轨道信息。
- **AIFX 归 `STEM/`**，命名保留 `Ai FX` 字样。
- **明确放弃序号补零**（`DX 1` 不改成 `DX 01`）：否则会把已交付成品改坏，
  与「已合规绝不动」的承诺冲突。

### jianying-draft-toolkit 2.5.0 —— 视频名提取 + 按视频分包

- 新增 `core/videoname.py`：从视频名解析 **项目 / 集数 / 编号 / AiFX**，
  命名模板新增 `{视频项目}` `{集数}` `{编号}` `{AiFX}` `{视频名}`。
- **纯数字**或**项目名超 4 字**时弹窗请用户补项目名（给默认建议值）；
  答案存 `video_project_answers`，下次不再问。
- **按视频分包**：视频轨每个片段一个文件夹，各音频轨按该片段时间窗切出、
  空白补静音保持对齐。实测 8 片段 / 6 轨 / 632s 草稿，逐段时长对齐。
- 分包模式下跳过 AAF（语义冲突）。

### 测试

- `tests/test_core_logic.py`：28 项（含 rename-unify 目标化后的新用例）
- 沙箱 `test_d2.py` 38/38、`smoke_gui_d2.py` 16/16、`test_d3.py` 17/17

---

## 1.1.0 — 2026-09-23

**工程配套改进。四个工具的功能行为未变。**

测试与质量
- **测试入库**：新增 `tests/`（15 模块导入自检 / 24 项核心逻辑 / 5 项接线 / 4 个 GUI 冒烟 /
  剪映端到端），此前这批脚本只在临时目录里，随时会丢
- **全部跨机化**：不再写死用户名与盘符（解释器走 `PT_TEST_PY` → 当前解释器 → 自动探测；
  草稿根走 `%LOCALAPPDATA%`；仓库根从文件位置反推）
- **结果分级**：`PASS / SKIP / FAIL / ERROR` 四态 + 退出码 `0/1/2`，
  区分「功能坏了」与「测试脚本跟不上接口变动」；缺可选依赖（如 py-ptsl）记 SKIP 而非 FAIL

构建与发布
- **新增 `tools/build.py`**：一条命令构建四工具，直调 `python -m PyInstaller` 并逐工具判返回码，
  失败时打印工具名 + 返回码 + 关键 stderr，**绝不静默**
- **四个 `build.ps1` 降级为转发薄壳**：原 325 行重复逻辑收敛（其中三份都在绕过
  「PowerShell 把 PyInstaller 的 stderr 当错误流」这个坑）
- 新增 `tools/verify_exe.py`：exe 产物核验（清单 + 真启动 + 顶层窗口枚举，查 `tk` 残留空窗）

版本
- **四工具统一口径**：`APP_VERSION = "X.Y.Z"`（不带 `v` 前缀）+ `build_date()`；
  窗口标题统一 `名称 vX.Y.Z (YYYY-MM-DD)`
- pt-tools 与 pt-project-folder-builder **补上版本号**（此前完全没有，界面无法辨别新旧）
- 新增仓库根 `VERSION` 文件，供 `tools/build.py` 命名出口目录

依赖提示
- pt-tools 在「技能脚本已内置、仅缺 venv」时报出**可操作指引**（此前含糊报「技能目录不存在」，
  指向了并非真正缺失的一环）

文档
- 新增三个工具级 `README.md` 与本 `CHANGELOG.md`
- 订正根 README 中已失效的「存在开发机硬编码路径」提示

---

## 1.0.1 — 2026-09-23

修复三处缺陷：

- **`jianying-draft-toolkit`：`init_config()` 静默丢配置**（真 bug）
  配置向导重建整个字典覆盖写盘，导致重跑 `--init`（或首次向导）会清空
  `name_templates` / `name_templates_active` / `export_mode` / `track_name_template` /
  `track_spec` / `export_aaf` / `aaf_media_mode`。改为三层合并落盘；
  顺带修了输出目录为空时的 `while` 死循环（连续回车会卡住）。
- **`pt-project-folder-builder`：复制步骤顺序**（数据丢失风险）
  原为「先 `os.remove` 旧文件、后判源是否存在」，模板 `.ptx` 被移走时会「删了不补」。
  改为先确认源可复制再动目标文件。
- **`pt-project-folder-builder/build.ps1`：写死用户名**
  解释器路径写死 `C:\Users\Administrator\...`，家用机直接失败。改为 `$env:USERNAME` 自适应。

---

## 1.0.0 — 2026-09-21

首次聚合发布，包含四个工具：

- **pt-tools** —— Pro Tools 自动化工具箱 GUI（扫描 / 导出 / 清理）
- **pt-project-folder-builder** —— PT 工程文件夹批量生成
- **jianying-draft-toolkit** —— 剪映草稿工具箱（导出 / 导入多轨 / 交付包）
- **rename-unify** —— 素材批量命名统一
